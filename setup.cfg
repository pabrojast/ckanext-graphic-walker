[metadata]
name = ckanext-graphic-walker
version = 0.1.0

[options]
packages = find_namespace:
namespace_packages = ckanext

[options.packages.find]
include = ckanext.*

[tool:pytest]
addopts = --cov=ckanext.graphic_walker
